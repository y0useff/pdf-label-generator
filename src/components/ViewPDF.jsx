import * as React from 'react'

export default function ViewPDF() {
    const node = document.getElementById("if")
    console.log(node)

    return(
        <>
        <p>{node}</p>
        </>
    )
}